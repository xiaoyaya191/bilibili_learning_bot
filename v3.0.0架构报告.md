# bilibili_learning_bot v3.0.0 架构报告

> 仓库: https://github.com/xiaoyaya191/bilibili_learning_bot  
> 版本: 3.0.0  
> 重构日期: 2026-06  
> 许可证: MIT  

---

## 一、重构目标与成果

v3.0.0 是一次彻底的架构重构，核心目标是将 16.8K 行的单体巨无霸拆分为模块化、可维护、可测试的代码库。

### 重构前后对比

| 指标 | 重构前 (v2.2.1) | 重构后 (v3.0.0) | 变化 |
|------|----------------|----------------|------|
| 总代码行数 | ~26,500 行 | ~33,000 行 | +25% (新增功能) |
| 入口文件 | `new_agent.py` (16,822 行) | `main.py` (164 行) | -99.9% |
| 模块数量 | 31 个 (混杂) | 80 个 (结构化) | +158% |
| 配置系统 | 2 套并行 | 1 套统一 | 简化 |
| 密码存储 | 明文 | SHA-256 哈希 | 安全提升 |
| 测试用例 | 0 | 43 个 (pytest) | 从零开始 |
| 死代码 | 存在 | 已清理 | 净化 |

---

## 二、核心架构设计

### 2.1 Mixin 还有一位第一个继承者 pattern (`brain/`)

```python
class AgentBrain(
    BrainInitMixin,      # __init__ 和属性初始化
    BrainAIMixin,        # AI 调用后端
    BrainRuntimeMixin,   # 运行时时钟和内存
    BrainUpsMixin,       # UP主记忆和喜欢
    BrainHistoryMixin,   # 视频历史管理
    BrainJournalMixin,   # 日记和学习日志
    BrainAutoMixin,      # 自动日记和自我进化
    BrainSessionMixin,   # 会话管理（精力/评论/私信/弹幕）
    BrainLearnMixin,     # 学习归档
    BrainCuriosityMixin, # 好奇心深度搜索
    BrainVideoMixin,     # 视频理解（字幕/ASR/视觉帧）
    BrainInteractMixin,  # 视觉分析与互动
    BrainLoopMixin,      # 主循环 run()
):
    pass  # 55 行 → 13 个 <1000 行的 mixin 组合
```

设计理由：
- 保持向后兼容的单体 API（`AgentBrain` 类名不变）
- 每个 mixin 职责单一，便于独立测试和维护
- 避免深层继承链，通过组合实现功能扩展

### 2.2 分层架构

```
┌─────────────────────────────────────────┐
│           CLI / Web Panel               │  # cli/app.py, web_panel.py
│         (用户交互层)                     │
├─────────────────────────────────────────┤
│            api/ (B站 API层)              │  # client.py, auth.py, subtitles.py
│              请求节流、WBI签名、风控fallback│
├─────────────────────────────────────────┤
│          brain/ (核心大脑)               │  # agent_brain.py + 13 mixins
│       调度、AI交互、学习、进化、互动       │
├─────────────────────────────────────────┤
│        services/ (业务服务层)            │  # agent_service, knowledge_tutor
│           video_to_ppt, interest_engine  │
├─────────────────────────────────────────┤
│        knowledge/ (知识库层)              │  # classifier, web_search, browse
│                                         │
│        persona/ (人格心理层)              │  # managers, psycho
│                                         │
│        security/ (安全层)                │  # guard
│                                         │
├─────────────────────────────────────────┤
│        utils/ (通用工具)                 │  # helpers, storage, display, lock
└─────────────────────────────────────────┘
```

---

## 三、模块详解

### 3.1 api/ — B站 API 层 (6 文件, ~1,650 行)

| 文件 | 职责 | 关键技术 |
|------|------|----------|
| `client.py` | BiliClient 主类 | HTTP/2 连接复用、WBI 签名缓存、视频元数据缓存 (5min TTL) |
| `auth.py` | 登录认证 | Cookie 校验、二维码登录、buvid3 自动修复 |
| `subtitles.py` | 字幕获取 | `player/wbi/v2` 412 风控 fallback、`player/v2` AI 字幕识别 |
| `throttle.py` | 请求节流器 | 基于 Token Bucket 的流量控制 |
| `compat.py` | 兼容层 | bilibili-api-python 新旧版本兼容 |

**关键技术点**：
- HTTP/2 连接复用：共享 `httpx.AsyncClient` 避免重复建连
- WBI 签名缓存：每小时内复用，减少 nav 接口调用
- buvid3 自动修复：标准 UUID+infoc 格式校验，畸形时自动生成

### 3.2 brain/ — 核心大脑 (21 文件, ~8,301 行)

#### 3.2.1 agent_brain.py (55 行)
纯组合类，13 个 mixin 的多重继承。零业务逻辑，所有职责委托给各个 mixin。

#### 3.2.2 Mixin 模块拆解

| Mixin | 文件 | 行数 | 核心职责 |
|-------|------|------|----------|
| BrainInitMixin | `_brain_init.py` | ~350 | 初始化、属性定义、配置加载 |
| BrainLoopMixin | `_brain_loop.py` | ~905 | 主循环 `run()`、状态机调度 |
| BrainVideoMixin | `_brain_video.py` | ~621 | 视频理解：字幕/ASR/视觉帧 |
| BrainSessionMixin | `_brain_session.py` | ~640 | 精力管理、评论/私信/弹幕/UP关注 |
| BrainAIMixin | `_brain_ai.py` | ~450 | LLM 调用封装、模型降级、成本记录 |
| BrainLearnMixin | `_brain_learn.py` | ~480 | 学习归档、知识库写入、三层分类 |
| BrainCuriosityMixin | `_brain_curiosity.py` | ~520 | 好奇心搜索、深度探索、兴趣匹配 |
| BrainInteractMixin | `_brain_interact.py` | ~410 | 视觉分析、评论生成、互动决策 |
| BrainJournalMixin | `_brain_journal.py` | ~380 | 日记记录、学习日志、行为追踪 |
| BrainAutoMixin | `_brain_auto.py` | ~290 | 自动日记、自我进化、人格成长 |
| BrainHistoryMixin | `_brain_history.py` | ~510 | 视频历史去重、观看记录、推荐去重 |
| BrainRuntimeMixin | `_brain_runtime.py` | ~320 | 运行时钟、内存监控、性能指标 |
| BrainUpsMixin | `_brain_ups.py` | ~380 | UP 主记忆、关注管理、偏好追踪 |

#### 3.2.3 独立模块

| 文件 | 职责 |
|------|------|
| `comment.py` | 评论互动管理（拉取/回复/发送/节奏控制） |
| `private_msg.py` | 私信处理（轮询/上下文/自动回复） |
| `video_analysis.py` | 手动视频分析（V 命令实现） |
| `standby.py` | 待机监听引擎 v2（@通知响应） |
| `monitor.py` | 实时监听引擎（独立于主循环） |

### 3.3 services/ — 业务服务层 (6 文件, ~2,990 行)

| 文件 | 职责 | 技术特点 |
|------|------|----------|
| `agent_service.py` | Agent 技能执行 | 规划→搜索→观看→总结 闭环 |
| `knowledge_tutor.py` | 知识辅导 | 讲解/问答/二次创作/HTML 卡片生成 |
| `video_to_ppt.py` | 视频→网页 | 19 种视觉风格，Claude 设计系统 |
| `interest_engine.py` | 智能兴趣引擎 | 多维度评分+同义词+排除词+AI建议 |
| `reply_safety.py` | 回复安全审查 | 关键词/政治敏感/注入防护 |
| `utils.py` | 工具函数 | 兴趣管理、通用工具 |

### 3.4 knowledge/ — 知识库层 (7 文件, ~2,706 行)

| 文件 | 职责 |
|------|------|
| `classifier.py` | 智能分类（三层分类体系） |
| `web_search.py` | 网络搜索 + AI 验证 |
| `browse.py` | 浏览整理 |
| `revisit.py` | 知识重温（记忆曲线） |
| `organize.py` | 一键整理知识库 |
| `custom.py` | 用户自定义知识管理 |

### 3.5 persona/ — 人格心理层 (2 文件, ~1,490 行)

| 文件 | 职责 |
|------|------|
| `managers.py` | 人格/心情/日记/进化管理器 |
| `psycho.py` | 心理画像引擎 + 推荐系统（PsychoProfile）">

### 3.6 security/ — 安全层 (2 文件, ~135 行)

| 文件 | 职责 |
|------|------|
| `guard.py` | 内容审查：关键词过滤 + 政治敏感拦截 + 提示词注入防护 |

### 3.7 utils/ — 通用工具 (新增，4 文件)

| 文件 | 职责 |
|------|------|
| `helpers.py` | URL 脱敏、文本处理、通用函数 |
| `storage.py` | JsonStore 线程安全 JSON 存储 + 脱敏导出 |
| `display.py` | 彩色日志/终端显示 |
| `lock.py` | 单实例锁（PID 文件 + 进程检测） |

---

## 四、v3.0.0 新增功能

### 4.1 🔴 @通知响应 (At-Reply Engine)

**场景**：在任何视频评论区发 "@bot 总结这个视频"，bot 自动识别并回复总结。

**实现**：
1. 通过 B站 `x/msg/at` API 拉取 `@我` 通知
2. 从评论上下文自动提取视频 BVid（无需手动提供）
3. 两种模式：通知模式 + legacy 模式

```
User: "@bot 总结一下这个视频"
Bot:  [自动识别所在视频] → [AI总结] → [回复评论]
```

**技术点**：
- 待机监听引擎 `standby.py` 独立运行，不干扰主循环
- 解析评论元数据关联视频信息

### 4.2 📡 实时监听模式 (Monitor Mode)

**特点**：独立于视频刷取的消息监听引擎
- 只盯私信和评论，有新消息立刻 AI 回复
- 不刷视频、不消耗精力、CPU 占用极低

**架构**：
```
Monitor Engine (monitor.py)
├── 私信轮询 (private_msg.py)
├── 评论拉取 (comment.py)  
└── @通知响应 (standby.py)
```

Web 面板新增「📡 实时监听」页面：
- 启停控制、配置管理、统计面板、实时日志

### 4.3 🎨 视频→网页 (video_to_ppt.py)

将已学视频生成精美 PPT 风格 HTML 页面：

**19 种视觉风格**：
- Claude 设计主题（默认）：毛玻璃卡片 + 数字滚动动画
- 数字滚动动画：easeOutExpo 缓动
- 模板目录：`templates/claude/`（6 个参考页面 + AI 设计规范）

**

---

## 五、安全设计

### 5.1 多层安全审查

```
输入内容
  → 关键词过滤 (blocked_keywords)
  → 政治敏感检测 (political content)
  → 提示词注入防护 (prompt injection)
  → 内容脱敏输出
```

### 5.2 隐私保护

| 措施 | 实现 |
|------|------|
| API Key 脱敏 | `mask_secret()` 显示为 `sk-***xxxx` |
| Cookie 脱敏 | 导出时替换为 `[已隐藏]` |
| 密码哈希 | SHA-256 + salt 存储 |
| 一键恢复出厂 | `R` 命令清除所有配置/日志/数据 |
| 原子写入 | tmp+replace 防止断电数据损坏 |

---

## 六、测试体系 (43 个 pytest)

```
tests/
├── test_api/           # API 层测试
│   ├── test_client.py
│   ├── test_auth.py
│   └── test_subtitles.py
├── test_brain/         # 大脑核心测试
│   ├── test_init.py
│   ├── test_loop.py
│   ├── test_video.py
│   └── test_session.py
├── test_services/      # 服务层测试
│   ├── test_agent.py
│   ├── test_knowledge.py
│   └── test_interest.py
├── test_knowledge/     # 知识库测试
│   ├── test_classifier.py
│   └── test_browse.py
└── test_security/      # 安全测试
    └── test_guard.py
```

---

## 七、运行方式

### 7.1 快速启动

```bash
# 主入口
python3 main.py

# CLI 模式
python3 start_cli.py

# Web 面板
python3 web_panel.py
```

### 7.2 Docker 部署

```bash
docker-compose up -d
```

### 7.3 菜单快捷键

| 按键 | 功能 |
|------|------|
| `1` | 🚀 启动机器人 |
| `L` | 📡 实时监听模式 |
| `V` | 📹 手动视频分析 |
| `T` | 🎓 知识辅导 |
| `W` | 🌐 网页讲解 |
| `P` | 🎨 视频→网页 |
| `N` | 📝 自定义知识管理 |
| `E` | 📤 导出配置（脱敏） |

---

## 八、核心技术点总结

| 技术领域 | 实现方案 |
|----------|----------|
| 架构模式 | Mixin 组合 + 分层架构 |
| API 通信 | httpx (HTTP/2) + bilibili-api-python v16 |
| AI 对话 | OpenAI API 兼容接口 (chat/completions) |
| 向量检索 | SentenceTransformer + 余弦相似度 |
| 视频理解 | 字幕 (player/wbi/v2) + ASR (FunASR/Whisper) + 视觉帧 |
| 知识存储 | Markdown 文件 + JSON 索引 |
| 配置管理 | 统一 JSON 配置 + `__getattr__` 动态属性 |
| 数据安全 | SHA-256 哈希 + 原子写入 + 自动脱敏 |
| 测试框架 | pytest (43 个测试用例) |

---

## 九、后续优化方向

基于 REFACTOR_PLAN.md 的展望：

1. **进一步拆分单体**：`cli/app.py` 仍较大 (~4,200 行)，可拆分为子命令模块
2. **异步全链路优化**：部分同步 I/O 可改为 async
3. **插件系统**：允许用户自定义插件扩展功能
4. **多账号支持**：`BILI_ACCOUNT_DATA_DIR` 环境变量已预留
5. **WebSocket 实时推送**：替代轮询，降低延迟

---

*报告生成时间: 2026-07-03*  
*基于版本: v3.0.0 (commit d8335aa)*