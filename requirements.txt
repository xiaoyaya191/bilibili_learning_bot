# ============================================================
# bilibili_learning_bot v3.0.0 依赖清单
# ============================================================
# ⚠️ 版本已锁定为开发者环境精确版本，保证兼容性。
#
# 🔧 Termux (Android) 用户：
#    PyYAML 是 bilibili-api-python 的间接依赖，会由 pip 自动安装。
#    如果遇到编译错误，请先运行：
#      pkg install libyaml libyaml-dev python python-pip
#    然后再执行 pip install -r requirements.txt

bilibili-api-python==17.4.1
openai==2.33.0
httpx==0.28.1
colorama==0.4.6
qrcode==8.2
qrcode-terminal==0.8
Pillow==12.1.0
Flask==3.1.2
flask-cors==6.0.2
requests==2.32.5
imageio-ffmpeg==0.6.0

# Optional: ASR 语音识别
# funasr==1.3.1
# torch==2.11.0
# torchaudio==2.11.0

# Optional: 向量检索
# sentence-transformers
# numpy==2.3.3
